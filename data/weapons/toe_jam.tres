[gd_resource type="Resource" script_class="WeaponDef" load_steps=2 format=3]

[ext_resource type="Script" path="res://resources/weapon_def.gd" id="1_def"]

[resource]
script = ExtResource("1_def")
display_name = "Toe Jam"
kind = 3
damage = 60.0
note = "Charged ram: arm it (bumper glows) and the next landed ram deals a flat 60 instead of speed-scaled crash damage. Held until it connects; still needs a real hit (above ram_min_speed)."
